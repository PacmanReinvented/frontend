package Interfaces.enums;

public enum InputTypes {
    //This is a separate enum from MoveDirection in the event you want more input than moving
    MOVEUP,
    MOVEDOWN,
    MOVERIGHT,
    MOVELEFT,
    //This isn't actually used, but used as a proof of concept to justify this being a separate enum
    SHOOT
}
