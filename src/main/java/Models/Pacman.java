package Models;

public class Pacman extends Character
{
    // fields

    // constructor
    public Pacman(int posX, int posY, int score, boolean vulnerable, boolean eaten)
    {
        super(posX, posY, score, vulnerable, eaten);
    }

    // properties

    // methods
}
